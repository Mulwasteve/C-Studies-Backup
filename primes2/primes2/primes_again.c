//using the primes pointers again
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main(void)
{
	unsigned long long *pPrimes = NULL;		//to hold the location on the heap to store the prime numbers
	unsigned long long trial = 0;			//number to be tested
	int total = 0;
	int count = 0;		//primes that have been found and stored

}